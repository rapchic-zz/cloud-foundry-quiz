# Github repos
 services:
  sample-repo:
    service_id: githubprivate
    parameters:
      repo_name: '{{toolchain.name}}'
      repo_url: 'https://github.com/rapchic/cloud-foundry-quiz'
      type: clone
      has_issues: true
      enable_traceability: true
