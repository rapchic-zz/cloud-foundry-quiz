# Github repos
 services:
  sample-repo:
    service_id: githubprivate
    parameters:
      repo_name: '{{toolchain.name}}'
      repo_url: 
      type: clone
      has_issues: true
      enable_traceability: true
